using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ControlLens : MonoBehaviour
{
    // Start is called before the first frame update
    //List<Vector3> fishShadowPosList = new List<Vector3>() { new Vector3(0.5f, -1f, 0), new Vector3(-1.8f, -1.4f, 0), new Vector3(1f, -2.5f, 0) };

    //Subscription<fish_hooked_event> fishHooked_event_subscription;
    //Subscription<pull_fish_event> pullFish_event_subscription;
    //Subscription<fish_escape_event> fishEscape_event_subscription;
    //[SerializeField]
    //private GameObject magnifyGlass;
    //private void Start()
    //{
    //    fishHooked_event_subscription = EventBus.Subscribe<fish_hooked_event>(handleFishHooked);
    //    pullFish_event_subscription = EventBus.Subscribe<pull_fish_event>(handlePullFish);
    //    fishEscape_event_subscription = EventBus.Subscribe<fish_escape_event>(handleFishEscape);
    //}

    //void handleFishHooked(fish_hooked_event e)
    //{
    //    magnifyGlass.transform.position = fishShadowPosList[Random.Range(0, 3)];
    //    magnifyGlass.SetActive(true);
    //}

    //void handlePullFish(pull_fish_event e)
    //{
    //    magnifyGlass.SetActive(false);
    //}

    //void handleFishEscape(fish_escape_event e)
    //{
    //    magnifyGlass.SetActive(false);
    //}

}
